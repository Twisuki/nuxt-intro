import type { ThemeConfig } from "../app/types/theme"

export default {
  site: {
    base: "/",
  },
  user: {
    name: "Twisuki",
    avatar: "/avatar.png",
    welcome: "Welcome to my site!",
  },
} as ThemeConfig
